{
  "name": "near-treasury",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"blue,green\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd nf-be && cargo watch -x run",
    "dev:frontend": "cd nt-fe && npm run dev -- -p 3001",
    "install:all": "cd nt-fe && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
